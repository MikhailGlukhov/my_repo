import 'package:flutter/material.dart';

class FixingList extends StatelessWidget {
  const FixingList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),backgroundColor: const Color.fromARGB(87, 228, 188, 215),);
  }
}